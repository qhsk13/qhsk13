# Example only: use this profile when you have a real PostgreSQL server.
# Add the PostgreSQL JDBC driver to pom.xml before using this profile.
# spring.datasource.url=jdbc:postgresql://localhost:5432/offline_messenger
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
