# Browser Extension Notification Checklist

브라우저 확장 프로그램의 바탕화면 알림은 아래 조건이 모두 맞아야 동작합니다.

## 반드시 해야 하는 작업

1. 서버를 먼저 실행합니다.

```bat
run-h2.bat
```

또는 선택한 DB 옵션에 맞게 실행합니다.

```bat
run-h2-postgres-mode.bat
run-hsqldb.bat
```

2. Chrome 또는 Edge 확장 관리 화면에서 확장 프로그램을 새로고침합니다.

```text
chrome://extensions
edge://extensions
```

3. `Offline Messenger` 확장 팝업을 한 번 엽니다.

4. 로그인합니다.

5. 팝업 왼쪽 내 정보 영역에서 `메시지 알림` 체크박스를 켭니다.

6. 브라우저 알림 권한과 Windows 알림 권한을 허용합니다.

## Chrome 알림 권한 허용

1. 주소창에 아래 주소를 입력합니다.

```text
chrome://settings/content/notifications
```

2. `사이트에서 알림 전송을 요청할 수 있음` 또는 알림 허용 옵션을 켭니다.

3. 차단 목록에 `Offline Messenger`, `chrome-extension://...`, 또는 관련 확장이 있으면 제거합니다.

4. 확장 관리 화면에서 `Offline Messenger`를 새로고침합니다.

## Edge 알림 권한 허용

1. 주소창에 아래 주소를 입력합니다.

```text
edge://settings/content/notifications
```

2. 알림 허용 옵션을 켭니다.

3. 차단 목록에 `Offline Messenger`, `extension://...`, 또는 관련 확장이 있으면 제거합니다.

4. 확장 관리 화면에서 `Offline Messenger`를 새로고침합니다.

## Windows 알림 권한 허용

Windows 10/11 기준:

1. `설정`을 엽니다.

2. `시스템`으로 이동합니다.

3. `알림`을 엽니다.

4. `알림` 전체 스위치를 켭니다.

5. 앱 목록에서 사용하는 브라우저를 찾습니다.

```text
Google Chrome
Microsoft Edge
```

6. 해당 브라우저의 알림을 켭니다.

7. `방해 금지`, `집중 지원`, `포커스`가 켜져 있으면 끕니다.

## 동작 방식

- 확장 팝업이 열려 있으면 WebSocket으로 새 메시지를 즉시 받습니다.
- 확장 팝업 창이 열려 있는 동안에는 바탕화면 알림을 띄우지 않습니다.
- 확장 팝업이 닫혀 있어도 브라우저가 실행 중이면 백그라운드에서 약 10초마다 서버를 확인합니다.
- 새 메시지가 있으면 바탕화면 알림을 띄웁니다.
- 바탕화면 알림을 클릭하면 메신저 팝업 창이 열리고 해당 채팅방으로 이동합니다.
- 본인이 보낸 메시지는 알림에서 제외됩니다.
- 알림 기능을 켠 직후 이미 있던 과거 메시지는 알림에서 제외됩니다.
- 서버가 꺼져 있거나 PC가 절전 상태면 알림을 받을 수 없습니다.

## 알림이 안 올 때 확인

1. 서버가 실행 중인지 확인합니다.

2. 확장 팝업에서 로그인이 유지되어 있는지 확인합니다.

3. `메시지 알림` 체크박스가 켜져 있는지 확인합니다.

4. Chrome/Edge 확장 관리 화면에서 확장 프로그램을 새로고침합니다.

5. 브라우저 알림 권한을 확인합니다.

6. Windows 알림 권한과 방해 금지/집중 지원 상태를 확인합니다.

7. 다른 사용자가 보낸 새 메시지로 테스트합니다.

## 배포 시 사용자 안내 문구

사용자에게 아래 내용을 안내하세요.

```text
1. 서버가 실행된 상태에서 브라우저 확장 프로그램을 설치/새로고침합니다.
2. 확장 팝업을 열고 로그인합니다.
3. 내 정보 영역의 메시지 알림을 켭니다.
4. 브라우저와 Windows 알림 권한을 허용합니다.
5. 팝업을 닫아도 브라우저가 켜져 있으면 약 10초 주기로 새 메시지 알림을 받습니다.
```
